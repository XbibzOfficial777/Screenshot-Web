{
  "name": "browser-screenshot-pro-backend",
  "version": "2.0.0",
  "description": "FastAPI backend for Browser Screenshot Pro",
  "main": "app/main.py",
  "scripts": {
    "start": "uvicorn app.main:app --host 0.0.0.0 --port 8000",
    "dev": "uvicorn app.main:app --reload --host 0.0.0.0 --port 8000",
    "test": "pytest",
    "lint": "flake8 app --count --select=E9,F63,F7,F82 --show-source --statistics"
  },
  "keywords": [
    "screenshot",
    "selenium",
    "fastapi",
    "browser-automation"
  ],
  "author": "Your Name",
  "license": "MIT"
}